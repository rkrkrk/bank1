<h1>Editing account</h1>
<p><i>(Must have Manager Login to delete account or change Account No. or balance)</i></p>

<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<p>
  <b>Account No:</b>
  <%= @account.accno %>
</p>


  <div class="field">
    <%= f.label :SortCode %><br />
    <%= f.number_field :sortcode %>
  </div>
  
  <p>
    <b>Balance:</b>
    <%= @account.balance %>
</p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', accounts_path %>

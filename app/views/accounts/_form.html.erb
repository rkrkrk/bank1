<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
    

  <% end %>
  <p>- Account No. must be 8 digit integer</br>
  - Sort Code must be 8 digit integer </br>
- Balance must be integer</br>
Anything else will return error</p>
  
 
  <div class="field">
    <%= f.label :Account_No %><br />
    <%= f.number_field :accno %>
  </div>
  <div class="field">
    <%= f.label :sortcode %><br />
    <%= f.number_field :sortcode %>
  </div>
  <div class="field">
    <%= f.label :balance %><br />
    <%= f.number_field :balance %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
